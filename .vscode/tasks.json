{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C using gcc",
            "type": "shell",
            "command": "gcc",
            "args": ["-Wall", "-o", "test", "test.c"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}